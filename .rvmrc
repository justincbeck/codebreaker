if [[ -d "$rvm_path/environments" && -s "$rvm_path/environments/ruby-1.9.2-p136@codebreaker" ]] ; then
  \. "$rvm_path/environments/ruby-1.9.2-p136@codebreaker"
else
  rvm --create use  "ruby-1.9.2-p136@codebreaker"
fi
